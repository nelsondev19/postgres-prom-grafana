apiVersion: 1

providers:
  - name: "PostgreSQL Database (CUSTOM)"
    orgId: 1
    folder: "Dashboards"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/postgres_custom
      foldersFromFilesStructure: true